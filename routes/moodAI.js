module.exports = function(req, res) {
    // This should return a static page describing the procedure being done to calculate the mood & song suggestions for
    // a user
    res.send({message: "This should return a static page describing the procedure being done to calculate the mood & song suggestions for a user "});
};
